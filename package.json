{
  "name": "global-vision-edu",
  "version": "1.0.0",
  "description": "Global Vision Education Consultants Landing Page",
  "private": true,
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^7.10.1",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "clean": "node -e \"const fs=require('fs'); const rm=p=>{try{fs.rmSync(p,{recursive:true,force:true});}catch(e){}}; ['build','dist','.cache','.parcel-cache','node_modules/.cache','.eslintcache'].forEach(rm); console.log('Cleaned build/tool caches.');\"",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
